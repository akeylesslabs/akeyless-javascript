/**
 * Akeyless API
 * The purpose of this application is to provide access to Akeyless API.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: support@akeyless.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FolderDeleteSync model module.
 * @module model/FolderDeleteSync
 * @version 5.0.33
 */
class FolderDeleteSync {
    /**
     * Constructs a new <code>FolderDeleteSync</code>.
     * @alias module:model/FolderDeleteSync
     * @param name {String} Folder name
     * @param uscName {String} Universal Secret Connector name
     */
    constructor(name, uscName) { 
        
        FolderDeleteSync.initialize(this, name, uscName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, uscName) { 
        obj['accessibility'] = 'regular';
        obj['delete-from-usc'] = false;
        obj['json'] = false;
        obj['name'] = name;
        obj['usc-name'] = uscName;
    }

    /**
     * Constructs a <code>FolderDeleteSync</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FolderDeleteSync} obj Optional instance to populate.
     * @return {module:model/FolderDeleteSync} The populated <code>FolderDeleteSync</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FolderDeleteSync();

            if (data.hasOwnProperty('accessibility')) {
                obj['accessibility'] = ApiClient.convertToType(data['accessibility'], 'String');
            }
            if (data.hasOwnProperty('delete-from-usc')) {
                obj['delete-from-usc'] = ApiClient.convertToType(data['delete-from-usc'], 'Boolean');
            }
            if (data.hasOwnProperty('json')) {
                obj['json'] = ApiClient.convertToType(data['json'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('remote-secret-name')) {
                obj['remote-secret-name'] = ApiClient.convertToType(data['remote-secret-name'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('uid-token')) {
                obj['uid-token'] = ApiClient.convertToType(data['uid-token'], 'String');
            }
            if (data.hasOwnProperty('usc-name')) {
                obj['usc-name'] = ApiClient.convertToType(data['usc-name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FolderDeleteSync</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FolderDeleteSync</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FolderDeleteSync.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['accessibility'] && !(typeof data['accessibility'] === 'string' || data['accessibility'] instanceof String)) {
            throw new Error("Expected the field `accessibility` to be a primitive type in the JSON string but got " + data['accessibility']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['remote-secret-name'] && !(typeof data['remote-secret-name'] === 'string' || data['remote-secret-name'] instanceof String)) {
            throw new Error("Expected the field `remote-secret-name` to be a primitive type in the JSON string but got " + data['remote-secret-name']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['uid-token'] && !(typeof data['uid-token'] === 'string' || data['uid-token'] instanceof String)) {
            throw new Error("Expected the field `uid-token` to be a primitive type in the JSON string but got " + data['uid-token']);
        }
        // ensure the json data is a string
        if (data['usc-name'] && !(typeof data['usc-name'] === 'string' || data['usc-name'] instanceof String)) {
            throw new Error("Expected the field `usc-name` to be a primitive type in the JSON string but got " + data['usc-name']);
        }

        return true;
    }


}

FolderDeleteSync.RequiredProperties = ["name", "usc-name"];

/**
 * for personal password manager
 * @member {String} accessibility
 * @default 'regular'
 */
FolderDeleteSync.prototype['accessibility'] = 'regular';

/**
 * Delete the secrets from the remote target usc as well
 * @member {Boolean} delete-from-usc
 * @default false
 */
FolderDeleteSync.prototype['delete-from-usc'] = false;

/**
 * Set output format to JSON
 * @member {Boolean} json
 * @default false
 */
FolderDeleteSync.prototype['json'] = false;

/**
 * Folder name
 * @member {String} name
 */
FolderDeleteSync.prototype['name'] = undefined;

/**
 * Remote Secret Name to delete when multiple syncs exist under the same USC
 * @member {String} remote-secret-name
 */
FolderDeleteSync.prototype['remote-secret-name'] = undefined;

/**
 * Authentication token (see `/auth` and `/configure`)
 * @member {String} token
 */
FolderDeleteSync.prototype['token'] = undefined;

/**
 * The universal identity token, Required only for universal_identity authentication
 * @member {String} uid-token
 */
FolderDeleteSync.prototype['uid-token'] = undefined;

/**
 * Universal Secret Connector name
 * @member {String} usc-name
 */
FolderDeleteSync.prototype['usc-name'] = undefined;






export default FolderDeleteSync;

