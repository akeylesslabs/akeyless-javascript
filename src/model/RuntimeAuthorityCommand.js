/**
 * Akeyless API
 * The purpose of this application is to provide access to Akeyless API.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: support@akeyless.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RuntimeAuthorityCommand model module.
 * @module model/RuntimeAuthorityCommand
 * @version 5.0.25
 */
class RuntimeAuthorityCommand {
    /**
     * Constructs a new <code>RuntimeAuthorityCommand</code>.
     * @alias module:model/RuntimeAuthorityCommand
     * @param agentId {String} Agent identifier for auditing
     * @param name {String} Secret name (dynamic secret or rotated secret)
     * @param payload {String} Query or action payload (SQL, natural language, or CLI-style)
     */
    constructor(agentId, name, payload) { 
        
        RuntimeAuthorityCommand.initialize(this, agentId, name, payload);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, agentId, name, payload) { 
        obj['agent-id'] = agentId;
        obj['json'] = false;
        obj['name'] = name;
        obj['payload'] = payload;
    }

    /**
     * Constructs a <code>RuntimeAuthorityCommand</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RuntimeAuthorityCommand} obj Optional instance to populate.
     * @return {module:model/RuntimeAuthorityCommand} The populated <code>RuntimeAuthorityCommand</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RuntimeAuthorityCommand();

            if (data.hasOwnProperty('agent-id')) {
                obj['agent-id'] = ApiClient.convertToType(data['agent-id'], 'String');
            }
            if (data.hasOwnProperty('json')) {
                obj['json'] = ApiClient.convertToType(data['json'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('payload')) {
                obj['payload'] = ApiClient.convertToType(data['payload'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('uid-token')) {
                obj['uid-token'] = ApiClient.convertToType(data['uid-token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RuntimeAuthorityCommand</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RuntimeAuthorityCommand</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RuntimeAuthorityCommand.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['agent-id'] && !(typeof data['agent-id'] === 'string' || data['agent-id'] instanceof String)) {
            throw new Error("Expected the field `agent-id` to be a primitive type in the JSON string but got " + data['agent-id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['payload'] && !(typeof data['payload'] === 'string' || data['payload'] instanceof String)) {
            throw new Error("Expected the field `payload` to be a primitive type in the JSON string but got " + data['payload']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['uid-token'] && !(typeof data['uid-token'] === 'string' || data['uid-token'] instanceof String)) {
            throw new Error("Expected the field `uid-token` to be a primitive type in the JSON string but got " + data['uid-token']);
        }

        return true;
    }


}

RuntimeAuthorityCommand.RequiredProperties = ["agent-id", "name", "payload"];

/**
 * Agent identifier for auditing
 * @member {String} agent-id
 */
RuntimeAuthorityCommand.prototype['agent-id'] = undefined;

/**
 * Set output format to JSON
 * @member {Boolean} json
 * @default false
 */
RuntimeAuthorityCommand.prototype['json'] = false;

/**
 * Secret name (dynamic secret or rotated secret)
 * @member {String} name
 */
RuntimeAuthorityCommand.prototype['name'] = undefined;

/**
 * Query or action payload (SQL, natural language, or CLI-style)
 * @member {String} payload
 */
RuntimeAuthorityCommand.prototype['payload'] = undefined;

/**
 * Authentication token (see `/auth` and `/configure`)
 * @member {String} token
 */
RuntimeAuthorityCommand.prototype['token'] = undefined;

/**
 * The universal identity token, Required only for universal_identity authentication
 * @member {String} uid-token
 */
RuntimeAuthorityCommand.prototype['uid-token'] = undefined;






export default RuntimeAuthorityCommand;

