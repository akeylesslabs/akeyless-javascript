/**
 * Akeyless API
 * The purpose of this application is to provide access to Akeyless API.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: support@akeyless.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PolicyOutput model module.
 * @module model/PolicyOutput
 * @version 5.0.25
 */
class PolicyOutput {
    /**
     * Constructs a new <code>PolicyOutput</code>.
     * it exposes object_types and intentionally hides enforce_on_items/enforce_on_targets.
     * @alias module:model/PolicyOutput
     */
    constructor() { 
        
        PolicyOutput.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PolicyOutput</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PolicyOutput} obj Optional instance to populate.
     * @return {module:model/PolicyOutput} The populated <code>PolicyOutput</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PolicyOutput();

            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'String');
            }
            if (data.hasOwnProperty('allowed_algorithms')) {
                obj['allowed_algorithms'] = ApiClient.convertToType(data['allowed_algorithms'], ['String']);
            }
            if (data.hasOwnProperty('allowed_key_names')) {
                obj['allowed_key_names'] = ApiClient.convertToType(data['allowed_key_names'], ['String']);
            }
            if (data.hasOwnProperty('allowed_key_types')) {
                obj['allowed_key_types'] = ApiClient.convertToType(data['allowed_key_types'], ['String']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('max_rotation_interval_days')) {
                obj['max_rotation_interval_days'] = ApiClient.convertToType(data['max_rotation_interval_days'], 'Number');
            }
            if (data.hasOwnProperty('object_types')) {
                obj['object_types'] = ApiClient.convertToType(data['object_types'], ['String']);
            }
            if (data.hasOwnProperty('path')) {
                obj['path'] = ApiClient.convertToType(data['path'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PolicyOutput</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PolicyOutput</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['account_id'] && !(typeof data['account_id'] === 'string' || data['account_id'] instanceof String)) {
            throw new Error("Expected the field `account_id` to be a primitive type in the JSON string but got " + data['account_id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['allowed_algorithms'])) {
            throw new Error("Expected the field `allowed_algorithms` to be an array in the JSON data but got " + data['allowed_algorithms']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['allowed_key_names'])) {
            throw new Error("Expected the field `allowed_key_names` to be an array in the JSON data but got " + data['allowed_key_names']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['allowed_key_types'])) {
            throw new Error("Expected the field `allowed_key_types` to be an array in the JSON data but got " + data['allowed_key_types']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['object_types'])) {
            throw new Error("Expected the field `object_types` to be an array in the JSON data but got " + data['object_types']);
        }
        // ensure the json data is a string
        if (data['path'] && !(typeof data['path'] === 'string' || data['path'] instanceof String)) {
            throw new Error("Expected the field `path` to be a primitive type in the JSON string but got " + data['path']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}



/**
 * @member {String} account_id
 */
PolicyOutput.prototype['account_id'] = undefined;

/**
 * @member {Array.<String>} allowed_algorithms
 */
PolicyOutput.prototype['allowed_algorithms'] = undefined;

/**
 * @member {Array.<String>} allowed_key_names
 */
PolicyOutput.prototype['allowed_key_names'] = undefined;

/**
 * @member {Array.<String>} allowed_key_types
 */
PolicyOutput.prototype['allowed_key_types'] = undefined;

/**
 * @member {String} id
 */
PolicyOutput.prototype['id'] = undefined;

/**
 * @member {Number} max_rotation_interval_days
 */
PolicyOutput.prototype['max_rotation_interval_days'] = undefined;

/**
 * @member {Array.<String>} object_types
 */
PolicyOutput.prototype['object_types'] = undefined;

/**
 * @member {String} path
 */
PolicyOutput.prototype['path'] = undefined;

/**
 * @member {String} type
 */
PolicyOutput.prototype['type'] = undefined;






export default PolicyOutput;

