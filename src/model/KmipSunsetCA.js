/**
 * Akeyless API
 * The purpose of this application is to provide access to Akeyless API.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: support@akeyless.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The KmipSunsetCA model module.
 * @module model/KmipSunsetCA
 * @version 5.0.33
 */
class KmipSunsetCA {
    /**
     * Constructs a new <code>KmipSunsetCA</code>.
     * @alias module:model/KmipSunsetCA
     * @param caId {String} CA ID to sunset
     */
    constructor(caId) { 
        
        KmipSunsetCA.initialize(this, caId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, caId) { 
        obj['ca-id'] = caId;
        obj['force'] = false;
        obj['json'] = false;
    }

    /**
     * Constructs a <code>KmipSunsetCA</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/KmipSunsetCA} obj Optional instance to populate.
     * @return {module:model/KmipSunsetCA} The populated <code>KmipSunsetCA</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new KmipSunsetCA();

            if (data.hasOwnProperty('ca-id')) {
                obj['ca-id'] = ApiClient.convertToType(data['ca-id'], 'String');
            }
            if (data.hasOwnProperty('force')) {
                obj['force'] = ApiClient.convertToType(data['force'], 'Boolean');
            }
            if (data.hasOwnProperty('grace-period')) {
                obj['grace-period'] = ApiClient.convertToType(data['grace-period'], 'Number');
            }
            if (data.hasOwnProperty('json')) {
                obj['json'] = ApiClient.convertToType(data['json'], 'Boolean');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('uid-token')) {
                obj['uid-token'] = ApiClient.convertToType(data['uid-token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>KmipSunsetCA</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>KmipSunsetCA</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of KmipSunsetCA.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['ca-id'] && !(typeof data['ca-id'] === 'string' || data['ca-id'] instanceof String)) {
            throw new Error("Expected the field `ca-id` to be a primitive type in the JSON string but got " + data['ca-id']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['uid-token'] && !(typeof data['uid-token'] === 'string' || data['uid-token'] instanceof String)) {
            throw new Error("Expected the field `uid-token` to be a primitive type in the JSON string but got " + data['uid-token']);
        }

        return true;
    }


}

KmipSunsetCA.RequiredProperties = ["ca-id"];

/**
 * CA ID to sunset
 * @member {String} ca-id
 */
KmipSunsetCA.prototype['ca-id'] = undefined;

/**
 * Force sunset even if issued clients or recent usage are detected
 * @member {Boolean} force
 * @default false
 */
KmipSunsetCA.prototype['force'] = false;

/**
 * Grace period in seconds for recent CA usage checks
 * @member {Number} grace-period
 */
KmipSunsetCA.prototype['grace-period'] = undefined;

/**
 * Set output format to JSON
 * @member {Boolean} json
 * @default false
 */
KmipSunsetCA.prototype['json'] = false;

/**
 * Authentication token (see `/auth` and `/configure`)
 * @member {String} token
 */
KmipSunsetCA.prototype['token'] = undefined;

/**
 * The universal identity token, Required only for universal_identity authentication
 * @member {String} uid-token
 */
KmipSunsetCA.prototype['uid-token'] = undefined;






export default KmipSunsetCA;

