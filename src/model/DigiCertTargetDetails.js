/**
 * Akeyless API
 * The purpose of this application is to provide access to Akeyless API.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: support@akeyless.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DigiCertTargetDetails model module.
 * @module model/DigiCertTargetDetails
 * @version 5.0.23
 */
class DigiCertTargetDetails {
    /**
     * Constructs a new <code>DigiCertTargetDetails</code>.
     * @alias module:model/DigiCertTargetDetails
     */
    constructor() { 
        
        DigiCertTargetDetails.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DigiCertTargetDetails</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DigiCertTargetDetails} obj Optional instance to populate.
     * @return {module:model/DigiCertTargetDetails} The populated <code>DigiCertTargetDetails</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DigiCertTargetDetails();

            if (data.hasOwnProperty('account_key_pem')) {
                obj['account_key_pem'] = ApiClient.convertToType(data['account_key_pem'], 'String');
            }
            if (data.hasOwnProperty('account_url')) {
                obj['account_url'] = ApiClient.convertToType(data['account_url'], 'String');
            }
            if (data.hasOwnProperty('challenge_type')) {
                obj['challenge_type'] = ApiClient.convertToType(data['challenge_type'], 'String');
            }
            if (data.hasOwnProperty('digicert_directory_type')) {
                obj['digicert_directory_type'] = ApiClient.convertToType(data['digicert_directory_type'], 'String');
            }
            if (data.hasOwnProperty('dns_target_name')) {
                obj['dns_target_name'] = ApiClient.convertToType(data['dns_target_name'], 'String');
            }
            if (data.hasOwnProperty('dns_target_type')) {
                obj['dns_target_type'] = ApiClient.convertToType(data['dns_target_type'], 'String');
            }
            if (data.hasOwnProperty('eab_hmac_key')) {
                obj['eab_hmac_key'] = ApiClient.convertToType(data['eab_hmac_key'], 'String');
            }
            if (data.hasOwnProperty('eab_key_id')) {
                obj['eab_key_id'] = ApiClient.convertToType(data['eab_key_id'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('gcp_project')) {
                obj['gcp_project'] = ApiClient.convertToType(data['gcp_project'], 'String');
            }
            if (data.hasOwnProperty('hosted_zone')) {
                obj['hosted_zone'] = ApiClient.convertToType(data['hosted_zone'], 'String');
            }
            if (data.hasOwnProperty('resource_group')) {
                obj['resource_group'] = ApiClient.convertToType(data['resource_group'], 'String');
            }
            if (data.hasOwnProperty('timeout')) {
                obj['timeout'] = ApiClient.convertToType(data['timeout'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DigiCertTargetDetails</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DigiCertTargetDetails</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['account_key_pem'] && !(typeof data['account_key_pem'] === 'string' || data['account_key_pem'] instanceof String)) {
            throw new Error("Expected the field `account_key_pem` to be a primitive type in the JSON string but got " + data['account_key_pem']);
        }
        // ensure the json data is a string
        if (data['account_url'] && !(typeof data['account_url'] === 'string' || data['account_url'] instanceof String)) {
            throw new Error("Expected the field `account_url` to be a primitive type in the JSON string but got " + data['account_url']);
        }
        // ensure the json data is a string
        if (data['challenge_type'] && !(typeof data['challenge_type'] === 'string' || data['challenge_type'] instanceof String)) {
            throw new Error("Expected the field `challenge_type` to be a primitive type in the JSON string but got " + data['challenge_type']);
        }
        // ensure the json data is a string
        if (data['digicert_directory_type'] && !(typeof data['digicert_directory_type'] === 'string' || data['digicert_directory_type'] instanceof String)) {
            throw new Error("Expected the field `digicert_directory_type` to be a primitive type in the JSON string but got " + data['digicert_directory_type']);
        }
        // ensure the json data is a string
        if (data['dns_target_name'] && !(typeof data['dns_target_name'] === 'string' || data['dns_target_name'] instanceof String)) {
            throw new Error("Expected the field `dns_target_name` to be a primitive type in the JSON string but got " + data['dns_target_name']);
        }
        // ensure the json data is a string
        if (data['dns_target_type'] && !(typeof data['dns_target_type'] === 'string' || data['dns_target_type'] instanceof String)) {
            throw new Error("Expected the field `dns_target_type` to be a primitive type in the JSON string but got " + data['dns_target_type']);
        }
        // ensure the json data is a string
        if (data['eab_hmac_key'] && !(typeof data['eab_hmac_key'] === 'string' || data['eab_hmac_key'] instanceof String)) {
            throw new Error("Expected the field `eab_hmac_key` to be a primitive type in the JSON string but got " + data['eab_hmac_key']);
        }
        // ensure the json data is a string
        if (data['eab_key_id'] && !(typeof data['eab_key_id'] === 'string' || data['eab_key_id'] instanceof String)) {
            throw new Error("Expected the field `eab_key_id` to be a primitive type in the JSON string but got " + data['eab_key_id']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['gcp_project'] && !(typeof data['gcp_project'] === 'string' || data['gcp_project'] instanceof String)) {
            throw new Error("Expected the field `gcp_project` to be a primitive type in the JSON string but got " + data['gcp_project']);
        }
        // ensure the json data is a string
        if (data['hosted_zone'] && !(typeof data['hosted_zone'] === 'string' || data['hosted_zone'] instanceof String)) {
            throw new Error("Expected the field `hosted_zone` to be a primitive type in the JSON string but got " + data['hosted_zone']);
        }
        // ensure the json data is a string
        if (data['resource_group'] && !(typeof data['resource_group'] === 'string' || data['resource_group'] instanceof String)) {
            throw new Error("Expected the field `resource_group` to be a primitive type in the JSON string but got " + data['resource_group']);
        }

        return true;
    }


}



/**
 * ACME Account Private Key (PEM-encoded). Supports ECDSA (P-256, P-384, P-521), RSA (2048+), and Ed25519. Auto-generated as ECDSA P-256 during first certificate issuance bootstrap. Stored encrypted, required for certificate operations and revocation.
 * @member {String} account_key_pem
 */
DigiCertTargetDetails.prototype['account_key_pem'] = undefined;

/**
 * ACME Account URL (returned after registration with DigiCert ACME). Used to retrieve existing account instead of re-registering.
 * @member {String} account_url
 */
DigiCertTargetDetails.prototype['account_url'] = undefined;

/**
 * ACMEChallengeType defines ACME challenge type for Let's Encrypt
 * @member {String} challenge_type
 */
DigiCertTargetDetails.prototype['challenge_type'] = undefined;

/**
 * @member {String} digicert_directory_type
 */
DigiCertTargetDetails.prototype['digicert_directory_type'] = undefined;

/**
 * Name of DNS target (transient field - not stored in DB). Used by CLI to pass DNS target name to SDK for creating target_object_assoc. Retrieved from target_object_assoc when reading target. Required when ChallengeType is dns.
 * @member {String} dns_target_name
 */
DigiCertTargetDetails.prototype['dns_target_name'] = undefined;

/**
 * @member {String} dns_target_type
 */
DigiCertTargetDetails.prototype['dns_target_type'] = undefined;

/**
 * External Account Binding HMAC key. Required until ACME account is bootstrapped on first issuance.
 * @member {String} eab_hmac_key
 */
DigiCertTargetDetails.prototype['eab_hmac_key'] = undefined;

/**
 * External Account Binding key identifier. Required until ACME account is bootstrapped on first issuance.
 * @member {String} eab_key_id
 */
DigiCertTargetDetails.prototype['eab_key_id'] = undefined;

/**
 * Email address for ACME account registration. Required.
 * @member {String} email
 */
DigiCertTargetDetails.prototype['email'] = undefined;

/**
 * GCP Cloud DNS: Project ID. Optional - can be derived from service account.
 * @member {String} gcp_project
 */
DigiCertTargetDetails.prototype['gcp_project'] = undefined;

/**
 * AWS Route53: Hosted zone ID. Required when DNSTargetType is AWS.
 * @member {String} hosted_zone
 */
DigiCertTargetDetails.prototype['hosted_zone'] = undefined;

/**
 * Azure DNS: Resource group name. Required when DNSTargetType is Azure.
 * @member {String} resource_group
 */
DigiCertTargetDetails.prototype['resource_group'] = undefined;

/**
 * A Duration represents the elapsed time between two instants as an int64 nanosecond count. The representation limits the largest representable duration to approximately 290 years.
 * @member {Number} timeout
 */
DigiCertTargetDetails.prototype['timeout'] = undefined;






export default DigiCertTargetDetails;

