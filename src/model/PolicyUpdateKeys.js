/**
 * Akeyless API
 * The purpose of this application is to provide access to Akeyless API.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: support@akeyless.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PolicyUpdateKeys model module.
 * @module model/PolicyUpdateKeys
 * @version 5.0.22
 */
class PolicyUpdateKeys {
    /**
     * Constructs a new <code>PolicyUpdateKeys</code>.
     * @alias module:model/PolicyUpdateKeys
     * @param id {String} Policy id
     */
    constructor(id) { 
        
        PolicyUpdateKeys.initialize(this, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id) { 
        obj['id'] = id;
        obj['json'] = false;
    }

    /**
     * Constructs a <code>PolicyUpdateKeys</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PolicyUpdateKeys} obj Optional instance to populate.
     * @return {module:model/PolicyUpdateKeys} The populated <code>PolicyUpdateKeys</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PolicyUpdateKeys();

            if (data.hasOwnProperty('allowed-algorithms')) {
                obj['allowed-algorithms'] = ApiClient.convertToType(data['allowed-algorithms'], ['String']);
            }
            if (data.hasOwnProperty('allowed-key-names')) {
                obj['allowed-key-names'] = ApiClient.convertToType(data['allowed-key-names'], ['String']);
            }
            if (data.hasOwnProperty('allowed-key-types')) {
                obj['allowed-key-types'] = ApiClient.convertToType(data['allowed-key-types'], ['String']);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('json')) {
                obj['json'] = ApiClient.convertToType(data['json'], 'Boolean');
            }
            if (data.hasOwnProperty('max-rotation-interval-days')) {
                obj['max-rotation-interval-days'] = ApiClient.convertToType(data['max-rotation-interval-days'], 'Number');
            }
            if (data.hasOwnProperty('object-types')) {
                obj['object-types'] = ApiClient.convertToType(data['object-types'], ['String']);
            }
            if (data.hasOwnProperty('path')) {
                obj['path'] = ApiClient.convertToType(data['path'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('uid-token')) {
                obj['uid-token'] = ApiClient.convertToType(data['uid-token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PolicyUpdateKeys</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PolicyUpdateKeys</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PolicyUpdateKeys.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['allowed-algorithms'])) {
            throw new Error("Expected the field `allowed-algorithms` to be an array in the JSON data but got " + data['allowed-algorithms']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['allowed-key-names'])) {
            throw new Error("Expected the field `allowed-key-names` to be an array in the JSON data but got " + data['allowed-key-names']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['allowed-key-types'])) {
            throw new Error("Expected the field `allowed-key-types` to be an array in the JSON data but got " + data['allowed-key-types']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['object-types'])) {
            throw new Error("Expected the field `object-types` to be an array in the JSON data but got " + data['object-types']);
        }
        // ensure the json data is a string
        if (data['path'] && !(typeof data['path'] === 'string' || data['path'] instanceof String)) {
            throw new Error("Expected the field `path` to be a primitive type in the JSON string but got " + data['path']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['uid-token'] && !(typeof data['uid-token'] === 'string' || data['uid-token'] instanceof String)) {
            throw new Error("Expected the field `uid-token` to be a primitive type in the JSON string but got " + data['uid-token']);
        }

        return true;
    }


}

PolicyUpdateKeys.RequiredProperties = ["id"];

/**
 * Specify allowed key algorithms (e.g., [RSA2048,AES128GCM])
 * @member {Array.<String>} allowed-algorithms
 */
PolicyUpdateKeys.prototype['allowed-algorithms'] = undefined;

/**
 * Specify allowed protection key names. To enforce using the account's default protection key, use 'default-account-key'
 * @member {Array.<String>} allowed-key-names
 */
PolicyUpdateKeys.prototype['allowed-key-names'] = undefined;

/**
 * Specify allowed key protection types (dfc, classic-key)
 * @member {Array.<String>} allowed-key-types
 */
PolicyUpdateKeys.prototype['allowed-key-types'] = undefined;

/**
 * Policy id
 * @member {String} id
 */
PolicyUpdateKeys.prototype['id'] = undefined;

/**
 * Set output format to JSON
 * @member {Boolean} json
 * @default false
 */
PolicyUpdateKeys.prototype['json'] = false;

/**
 * Set the maximum rotation interval for automatic key rotation.
 * @member {Number} max-rotation-interval-days
 */
PolicyUpdateKeys.prototype['max-rotation-interval-days'] = undefined;

/**
 * The object type this policy will apply to (items, targets)
 * @member {Array.<String>} object-types
 */
PolicyUpdateKeys.prototype['object-types'] = undefined;

/**
 * The path the policy refers to
 * @member {String} path
 */
PolicyUpdateKeys.prototype['path'] = undefined;

/**
 * Authentication token (see `/auth` and `/configure`)
 * @member {String} token
 */
PolicyUpdateKeys.prototype['token'] = undefined;

/**
 * The universal identity token, Required only for universal_identity authentication
 * @member {String} uid-token
 */
PolicyUpdateKeys.prototype['uid-token'] = undefined;






export default PolicyUpdateKeys;

