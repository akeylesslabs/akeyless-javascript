/**
 * Akeyless API
 * The purpose of this application is to provide access to Akeyless API.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: support@akeyless.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RotateKey model module.
 * @module model/RotateKey
 * @version 5.0.15
 */
class RotateKey {
    /**
     * Constructs a new <code>RotateKey</code>.
     * of it.
     * @alias module:model/RotateKey
     * @param name {String} Key name
     */
    constructor(name) { 
        
        RotateKey.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['json'] = false;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>RotateKey</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RotateKey} obj Optional instance to populate.
     * @return {module:model/RotateKey} The populated <code>RotateKey</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RotateKey();

            if (data.hasOwnProperty('json')) {
                obj['json'] = ApiClient.convertToType(data['json'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('new-cert-pem-data')) {
                obj['new-cert-pem-data'] = ApiClient.convertToType(data['new-cert-pem-data'], 'String');
            }
            if (data.hasOwnProperty('new-key-data')) {
                obj['new-key-data'] = ApiClient.convertToType(data['new-key-data'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('uid-token')) {
                obj['uid-token'] = ApiClient.convertToType(data['uid-token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RotateKey</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RotateKey</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RotateKey.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['new-cert-pem-data'] && !(typeof data['new-cert-pem-data'] === 'string' || data['new-cert-pem-data'] instanceof String)) {
            throw new Error("Expected the field `new-cert-pem-data` to be a primitive type in the JSON string but got " + data['new-cert-pem-data']);
        }
        // ensure the json data is a string
        if (data['new-key-data'] && !(typeof data['new-key-data'] === 'string' || data['new-key-data'] instanceof String)) {
            throw new Error("Expected the field `new-key-data` to be a primitive type in the JSON string but got " + data['new-key-data']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['uid-token'] && !(typeof data['uid-token'] === 'string' || data['uid-token'] instanceof String)) {
            throw new Error("Expected the field `uid-token` to be a primitive type in the JSON string but got " + data['uid-token']);
        }

        return true;
    }


}

RotateKey.RequiredProperties = ["name"];

/**
 * Set output format to JSON
 * @member {Boolean} json
 * @default false
 */
RotateKey.prototype['json'] = false;

/**
 * Key name
 * @member {String} name
 */
RotateKey.prototype['name'] = undefined;

/**
 * The new pem encoded certificate for the classic key. relevant only for keys provided by user ('bring-your-own-key')
 * @member {String} new-cert-pem-data
 */
RotateKey.prototype['new-cert-pem-data'] = undefined;

/**
 * The new base64 encoded value for the classic key. relevant only for keys provided by user ('bring-your-own-key')
 * @member {String} new-key-data
 */
RotateKey.prototype['new-key-data'] = undefined;

/**
 * Authentication token (see `/auth` and `/configure`)
 * @member {String} token
 */
RotateKey.prototype['token'] = undefined;

/**
 * The universal identity token, Required only for universal_identity authentication
 * @member {String} uid-token
 */
RotateKey.prototype['uid-token'] = undefined;






export default RotateKey;

