/**
 * Akeyless API
 * The purpose of this application is to provide access to Akeyless API.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: support@akeyless.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FileInfo model module.
 * @module model/FileInfo
 * @version 5.0.32
 */
class FileInfo {
    /**
     * Constructs a new <code>FileInfo</code>.
     * @alias module:model/FileInfo
     */
    constructor() { 
        
        FileInfo.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>FileInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FileInfo} obj Optional instance to populate.
     * @return {module:model/FileInfo} The populated <code>FileInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FileInfo();

            if (data.hasOwnProperty('blob_storage_key')) {
                obj['blob_storage_key'] = ApiClient.convertToType(data['blob_storage_key'], 'String');
            }
            if (data.hasOwnProperty('client_file_sha256')) {
                obj['client_file_sha256'] = ApiClient.convertToType(data['client_file_sha256'], 'String');
            }
            if (data.hasOwnProperty('content_type')) {
                obj['content_type'] = ApiClient.convertToType(data['content_type'], 'String');
            }
            if (data.hasOwnProperty('derivation_data')) {
                obj['derivation_data'] = ApiClient.convertToType(data['derivation_data'], 'String');
            }
            if (data.hasOwnProperty('encrypted_blob_sha256')) {
                obj['encrypted_blob_sha256'] = ApiClient.convertToType(data['encrypted_blob_sha256'], 'String');
            }
            if (data.hasOwnProperty('encrypted_size_bytes')) {
                obj['encrypted_size_bytes'] = ApiClient.convertToType(data['encrypted_size_bytes'], 'Number');
            }
            if (data.hasOwnProperty('etag_at_complete')) {
                obj['etag_at_complete'] = ApiClient.convertToType(data['etag_at_complete'], 'String');
            }
            if (data.hasOwnProperty('extension')) {
                obj['extension'] = ApiClient.convertToType(data['extension'], 'String');
            }
            if (data.hasOwnProperty('file_id')) {
                obj['file_id'] = ApiClient.convertToType(data['file_id'], 'String');
            }
            if (data.hasOwnProperty('filename')) {
                obj['filename'] = ApiClient.convertToType(data['filename'], 'String');
            }
            if (data.hasOwnProperty('plain_size_bytes')) {
                obj['plain_size_bytes'] = ApiClient.convertToType(data['plain_size_bytes'], 'Number');
            }
            if (data.hasOwnProperty('previous_active_file_info')) {
                obj['previous_active_file_info'] = FileInfo.constructFromObject(data['previous_active_file_info']);
            }
            if (data.hasOwnProperty('previous_blob_storage_key')) {
                obj['previous_blob_storage_key'] = ApiClient.convertToType(data['previous_blob_storage_key'], 'String');
            }
            if (data.hasOwnProperty('protection_key_name')) {
                obj['protection_key_name'] = ApiClient.convertToType(data['protection_key_name'], 'String');
            }
            if (data.hasOwnProperty('protection_key_version')) {
                obj['protection_key_version'] = ApiClient.convertToType(data['protection_key_version'], 'Number');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('transaction_owner')) {
                obj['transaction_owner'] = ApiClient.convertToType(data['transaction_owner'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FileInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FileInfo</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['blob_storage_key'] && !(typeof data['blob_storage_key'] === 'string' || data['blob_storage_key'] instanceof String)) {
            throw new Error("Expected the field `blob_storage_key` to be a primitive type in the JSON string but got " + data['blob_storage_key']);
        }
        // ensure the json data is a string
        if (data['client_file_sha256'] && !(typeof data['client_file_sha256'] === 'string' || data['client_file_sha256'] instanceof String)) {
            throw new Error("Expected the field `client_file_sha256` to be a primitive type in the JSON string but got " + data['client_file_sha256']);
        }
        // ensure the json data is a string
        if (data['content_type'] && !(typeof data['content_type'] === 'string' || data['content_type'] instanceof String)) {
            throw new Error("Expected the field `content_type` to be a primitive type in the JSON string but got " + data['content_type']);
        }
        // ensure the json data is a string
        if (data['derivation_data'] && !(typeof data['derivation_data'] === 'string' || data['derivation_data'] instanceof String)) {
            throw new Error("Expected the field `derivation_data` to be a primitive type in the JSON string but got " + data['derivation_data']);
        }
        // ensure the json data is a string
        if (data['encrypted_blob_sha256'] && !(typeof data['encrypted_blob_sha256'] === 'string' || data['encrypted_blob_sha256'] instanceof String)) {
            throw new Error("Expected the field `encrypted_blob_sha256` to be a primitive type in the JSON string but got " + data['encrypted_blob_sha256']);
        }
        // ensure the json data is a string
        if (data['etag_at_complete'] && !(typeof data['etag_at_complete'] === 'string' || data['etag_at_complete'] instanceof String)) {
            throw new Error("Expected the field `etag_at_complete` to be a primitive type in the JSON string but got " + data['etag_at_complete']);
        }
        // ensure the json data is a string
        if (data['extension'] && !(typeof data['extension'] === 'string' || data['extension'] instanceof String)) {
            throw new Error("Expected the field `extension` to be a primitive type in the JSON string but got " + data['extension']);
        }
        // ensure the json data is a string
        if (data['file_id'] && !(typeof data['file_id'] === 'string' || data['file_id'] instanceof String)) {
            throw new Error("Expected the field `file_id` to be a primitive type in the JSON string but got " + data['file_id']);
        }
        // ensure the json data is a string
        if (data['filename'] && !(typeof data['filename'] === 'string' || data['filename'] instanceof String)) {
            throw new Error("Expected the field `filename` to be a primitive type in the JSON string but got " + data['filename']);
        }
        // validate the optional field `previous_active_file_info`
        if (data['previous_active_file_info']) { // data not null
          FileInfo.validateJSON(data['previous_active_file_info']);
        }
        // ensure the json data is a string
        if (data['previous_blob_storage_key'] && !(typeof data['previous_blob_storage_key'] === 'string' || data['previous_blob_storage_key'] instanceof String)) {
            throw new Error("Expected the field `previous_blob_storage_key` to be a primitive type in the JSON string but got " + data['previous_blob_storage_key']);
        }
        // ensure the json data is a string
        if (data['protection_key_name'] && !(typeof data['protection_key_name'] === 'string' || data['protection_key_name'] instanceof String)) {
            throw new Error("Expected the field `protection_key_name` to be a primitive type in the JSON string but got " + data['protection_key_name']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['transaction_owner'] && !(typeof data['transaction_owner'] === 'string' || data['transaction_owner'] instanceof String)) {
            throw new Error("Expected the field `transaction_owner` to be a primitive type in the JSON string but got " + data['transaction_owner']);
        }

        return true;
    }


}



/**
 * @member {String} blob_storage_key
 */
FileInfo.prototype['blob_storage_key'] = undefined;

/**
 * @member {String} client_file_sha256
 */
FileInfo.prototype['client_file_sha256'] = undefined;

/**
 * @member {String} content_type
 */
FileInfo.prototype['content_type'] = undefined;

/**
 * @member {String} derivation_data
 */
FileInfo.prototype['derivation_data'] = undefined;

/**
 * @member {String} encrypted_blob_sha256
 */
FileInfo.prototype['encrypted_blob_sha256'] = undefined;

/**
 * @member {Number} encrypted_size_bytes
 */
FileInfo.prototype['encrypted_size_bytes'] = undefined;

/**
 * @member {String} etag_at_complete
 */
FileInfo.prototype['etag_at_complete'] = undefined;

/**
 * @member {String} extension
 */
FileInfo.prototype['extension'] = undefined;

/**
 * @member {String} file_id
 */
FileInfo.prototype['file_id'] = undefined;

/**
 * @member {String} filename
 */
FileInfo.prototype['filename'] = undefined;

/**
 * @member {Number} plain_size_bytes
 */
FileInfo.prototype['plain_size_bytes'] = undefined;

/**
 * @member {module:model/FileInfo} previous_active_file_info
 */
FileInfo.prototype['previous_active_file_info'] = undefined;

/**
 * @member {String} previous_blob_storage_key
 */
FileInfo.prototype['previous_blob_storage_key'] = undefined;

/**
 * @member {String} protection_key_name
 */
FileInfo.prototype['protection_key_name'] = undefined;

/**
 * @member {Number} protection_key_version
 */
FileInfo.prototype['protection_key_version'] = undefined;

/**
 * @member {String} status
 */
FileInfo.prototype['status'] = undefined;

/**
 * @member {String} transaction_owner
 */
FileInfo.prototype['transaction_owner'] = undefined;






export default FileInfo;

