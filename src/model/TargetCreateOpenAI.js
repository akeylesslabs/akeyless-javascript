/**
 * Akeyless API
 * The purpose of this application is to provide access to Akeyless API.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: support@akeyless.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TargetCreateOpenAI model module.
 * @module model/TargetCreateOpenAI
 * @version 5.0.9
 */
class TargetCreateOpenAI {
    /**
     * Constructs a new <code>TargetCreateOpenAI</code>.
     * targetCreateOpenAI is a command that creates a new openai target
     * @alias module:model/TargetCreateOpenAI
     * @param name {String} Target name
     */
    constructor(name) { 
        
        TargetCreateOpenAI.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['json'] = false;
        obj['name'] = name;
        obj['openai-url'] = 'https://api.openai.com/v1';
    }

    /**
     * Constructs a <code>TargetCreateOpenAI</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TargetCreateOpenAI} obj Optional instance to populate.
     * @return {module:model/TargetCreateOpenAI} The populated <code>TargetCreateOpenAI</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TargetCreateOpenAI();

            if (data.hasOwnProperty('api-key')) {
                obj['api-key'] = ApiClient.convertToType(data['api-key'], 'String');
            }
            if (data.hasOwnProperty('api-key-id')) {
                obj['api-key-id'] = ApiClient.convertToType(data['api-key-id'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('json')) {
                obj['json'] = ApiClient.convertToType(data['json'], 'Boolean');
            }
            if (data.hasOwnProperty('key')) {
                obj['key'] = ApiClient.convertToType(data['key'], 'String');
            }
            if (data.hasOwnProperty('max-versions')) {
                obj['max-versions'] = ApiClient.convertToType(data['max-versions'], 'String');
            }
            if (data.hasOwnProperty('model')) {
                obj['model'] = ApiClient.convertToType(data['model'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('openai-url')) {
                obj['openai-url'] = ApiClient.convertToType(data['openai-url'], 'String');
            }
            if (data.hasOwnProperty('organization-id')) {
                obj['organization-id'] = ApiClient.convertToType(data['organization-id'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('uid-token')) {
                obj['uid-token'] = ApiClient.convertToType(data['uid-token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TargetCreateOpenAI</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TargetCreateOpenAI</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TargetCreateOpenAI.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['api-key'] && !(typeof data['api-key'] === 'string' || data['api-key'] instanceof String)) {
            throw new Error("Expected the field `api-key` to be a primitive type in the JSON string but got " + data['api-key']);
        }
        // ensure the json data is a string
        if (data['api-key-id'] && !(typeof data['api-key-id'] === 'string' || data['api-key-id'] instanceof String)) {
            throw new Error("Expected the field `api-key-id` to be a primitive type in the JSON string but got " + data['api-key-id']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['key'] && !(typeof data['key'] === 'string' || data['key'] instanceof String)) {
            throw new Error("Expected the field `key` to be a primitive type in the JSON string but got " + data['key']);
        }
        // ensure the json data is a string
        if (data['max-versions'] && !(typeof data['max-versions'] === 'string' || data['max-versions'] instanceof String)) {
            throw new Error("Expected the field `max-versions` to be a primitive type in the JSON string but got " + data['max-versions']);
        }
        // ensure the json data is a string
        if (data['model'] && !(typeof data['model'] === 'string' || data['model'] instanceof String)) {
            throw new Error("Expected the field `model` to be a primitive type in the JSON string but got " + data['model']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['openai-url'] && !(typeof data['openai-url'] === 'string' || data['openai-url'] instanceof String)) {
            throw new Error("Expected the field `openai-url` to be a primitive type in the JSON string but got " + data['openai-url']);
        }
        // ensure the json data is a string
        if (data['organization-id'] && !(typeof data['organization-id'] === 'string' || data['organization-id'] instanceof String)) {
            throw new Error("Expected the field `organization-id` to be a primitive type in the JSON string but got " + data['organization-id']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['uid-token'] && !(typeof data['uid-token'] === 'string' || data['uid-token'] instanceof String)) {
            throw new Error("Expected the field `uid-token` to be a primitive type in the JSON string but got " + data['uid-token']);
        }

        return true;
    }


}

TargetCreateOpenAI.RequiredProperties = ["name"];

/**
 * API key for OpenAI
 * @member {String} api-key
 */
TargetCreateOpenAI.prototype['api-key'] = undefined;

/**
 * API key ID
 * @member {String} api-key-id
 */
TargetCreateOpenAI.prototype['api-key-id'] = undefined;

/**
 * Description of the object
 * @member {String} description
 */
TargetCreateOpenAI.prototype['description'] = undefined;

/**
 * Set output format to JSON
 * @member {Boolean} json
 * @default false
 */
TargetCreateOpenAI.prototype['json'] = false;

/**
 * The name of a key that used to encrypt the target secret value (if empty, the account default protectionKey key will be used)
 * @member {String} key
 */
TargetCreateOpenAI.prototype['key'] = undefined;

/**
 * Set the maximum number of versions, limited by the account settings defaults.
 * @member {String} max-versions
 */
TargetCreateOpenAI.prototype['max-versions'] = undefined;

/**
 * Default model to use with OpenAI
 * @member {String} model
 */
TargetCreateOpenAI.prototype['model'] = undefined;

/**
 * Target name
 * @member {String} name
 */
TargetCreateOpenAI.prototype['name'] = undefined;

/**
 * Base URL of the OpenAI API
 * @member {String} openai-url
 * @default 'https://api.openai.com/v1'
 */
TargetCreateOpenAI.prototype['openai-url'] = 'https://api.openai.com/v1';

/**
 * Organization ID
 * @member {String} organization-id
 */
TargetCreateOpenAI.prototype['organization-id'] = undefined;

/**
 * Authentication token (see `/auth` and `/configure`)
 * @member {String} token
 */
TargetCreateOpenAI.prototype['token'] = undefined;

/**
 * The universal identity token, Required only for universal_identity authentication
 * @member {String} uid-token
 */
TargetCreateOpenAI.prototype['uid-token'] = undefined;






export default TargetCreateOpenAI;

