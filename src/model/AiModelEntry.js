/**
 * Akeyless API
 * The purpose of this application is to provide access to Akeyless API.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: support@akeyless.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AiModelEntry model module.
 * @module model/AiModelEntry
 * @version 5.0.36
 */
class AiModelEntry {
    /**
     * Constructs a new <code>AiModelEntry</code>.
     * AiModelEntry is a single configured AI model: which Akeyless target holds the credentials, which LLM to call on it, and what part it plays in policy evaluation.
     * @alias module:model/AiModelEntry
     */
    constructor() { 
        
        AiModelEntry.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>AiModelEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AiModelEntry} obj Optional instance to populate.
     * @return {module:model/AiModelEntry} The populated <code>AiModelEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AiModelEntry();

            if (data.hasOwnProperty('model')) {
                obj['model'] = ApiClient.convertToType(data['model'], 'String');
            }
            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
            if (data.hasOwnProperty('target_id')) {
                obj['target_id'] = ApiClient.convertToType(data['target_id'], 'Number');
            }
            if (data.hasOwnProperty('target_name')) {
                obj['target_name'] = ApiClient.convertToType(data['target_name'], 'String');
            }
            if (data.hasOwnProperty('target_type')) {
                obj['target_type'] = ApiClient.convertToType(data['target_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AiModelEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AiModelEntry</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['model'] && !(typeof data['model'] === 'string' || data['model'] instanceof String)) {
            throw new Error("Expected the field `model` to be a primitive type in the JSON string but got " + data['model']);
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }
        // ensure the json data is a string
        if (data['target_name'] && !(typeof data['target_name'] === 'string' || data['target_name'] instanceof String)) {
            throw new Error("Expected the field `target_name` to be a primitive type in the JSON string but got " + data['target_name']);
        }
        // ensure the json data is a string
        if (data['target_type'] && !(typeof data['target_type'] === 'string' || data['target_type'] instanceof String)) {
            throw new Error("Expected the field `target_type` to be a primitive type in the JSON string but got " + data['target_type']);
        }

        return true;
    }


}



/**
 * @member {String} model
 */
AiModelEntry.prototype['model'] = undefined;

/**
 * Provider is display-only: it carries the provider label the model catalog reported for Model (meaningful for Bedrock, which fronts several providers) so the console can render its Provider column without re-fetching the whole catalog per row. Never used for credential resolution or validation - the target type is the authority there.
 * @member {String} provider
 */
AiModelEntry.prototype['provider'] = undefined;

/**
 * A gateway has exactly one AiModelRoleDefault entry whenever any entry exists: it serves AI Insight, ISI, and the standard policy decisions in Secretless AI / ARA. AiModelRoleQuorum entries are consulted alongside the default to double-check policy decisions before a risky action is allowed.
 * @member {String} role
 */
AiModelEntry.prototype['role'] = undefined;

/**
 * TargetId uses the repo-wide spelling (see types.Target.TargetId and AiInsightsConfigPart.TargetId) rather than the Go-idiomatic TargetID.
 * @member {Number} target_id
 */
AiModelEntry.prototype['target_id'] = undefined;

/**
 * @member {String} target_name
 */
AiModelEntry.prototype['target_name'] = undefined;

/**
 * TargetType is display-only, captured at write time like Provider. The console shows it as a read-only column; resolving it per row from the targets list instead would mean an extra lookup that can disagree with what the entry was created against.
 * @member {String} target_type
 */
AiModelEntry.prototype['target_type'] = undefined;






export default AiModelEntry;

